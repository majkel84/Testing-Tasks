#pragma once

#include <vector>
#include <iostream>

using namespace std;

class Arrays
{
public:
    vector<int> cyclicRotation(vector<int>& A, int K);
    int OddOccurrencesInArray(vector<int> &A);
};
