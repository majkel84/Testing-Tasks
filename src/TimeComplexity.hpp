#pragma once

#include <vector>
#include <iostream>

using namespace std;

class TimeComplexity
{
public:
    int countFrogJumps(int start, int finish, int stepSize);
    int permMissingElem(vector<int> &A);
    int tapeEquilibrum(vector<int> &A);
};
