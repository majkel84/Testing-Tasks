#pragma once

#include <vector>
#include <iostream>

using namespace std;

class PermCheck
{
public:
    int checkIsPermut(vector<int> &A);
    int FrogRiverOne(int X, vector<int>& A);
    vector<int> maxCounter(int N, vector<int> &A);
    int missingInteger(vector<int> &A);
};
